name: Basic Test

on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: List files
        run: |
          echo "Files in repository:"
          ls -la
      
      - name: Create test file
        run: |
          echo "Hello LPU" > test.txt
          echo "Test file created"
